version: '3.8'

services:
  mysqldb:
    image: mysql:5.7
    container_name: mysqldb
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: restapi
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql

  api-instance-1:
    build: .
    container_name: restapi_app_1
    restart: on-failure
    depends_on:
      - mysqldb
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysqldb:3306/restapi?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SERVER_FORWARD_HEADERS_STRATEGY: framework # for X-Forwarded-For

  api-instance-2:
    build: .
    container_name: restapi_app_2
    restart: on-failure
    depends_on:
      - mysqldb
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysqldb:3306/restapi?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SERVER_FORWARD_HEADERS_STRATEGY: framework # for X-Forwarded-For

  # Nginx Load Balancer
  nginx:
    image: nginx:alpine
    container_name: nginx_lb
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - api-instance-1
      - api-instance-2

volumes:
  db_data:
